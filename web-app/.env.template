# ----------------------------------------------------------------------------------------
# ■ Environment variables for `web-app`
# - Template for Infisical secrets that will overwrite corresponding env file.
# ----------------------------------------------------------------------------------------
{{"\n"}}
{{- /* Rendered to web-app/.env by `bun run secret:pull`. Dumps the /web-app folder; shared dev topology (host + ports) is pulled in via Infisical secret references, expanded at export time. vite.config.ts reads these via loadEnv. Edit in Infisical, not .env. */ -}}
{{- $pid := "266ba9d2-b8b3-41cd-a323-78da4a14da70" -}}

{{- range secret $pid "dev" "/web-app" -}}
{{ if .Comment }}# {{ .Comment }}
{{ end }}{{ .Key }}={{ .Value }}
{{ end -}}
