# Default: the docker-compose Postgres from `bun run db:up`.
# Fallback (native Homebrew Postgres): postgres://<macos-user>@localhost:5432/meow
DATABASE_URL=postgres://meow:meow@localhost:5432/meow
# openssl rand -hex 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
