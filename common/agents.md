# About
- This is a collection of shared utilities across the monorepo.
- We conform to the WinterTC (used to be WinterCG) guidelines.
  - If there's motivation for libraries targetting specifically the browser or server, 
    then we should have a `common-server` or `common-web` project.
