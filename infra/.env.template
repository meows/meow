# ----------------------------------------------------------------------------------------
# ■ Environment variables for the Caddy gateway (infra)
# - Template for Infisical secrets that will overwrite corresponding env file.
# ----------------------------------------------------------------------------------------
{{"\n"}}
{{- /* Rendered to infra/.env by `bun run secret:pull`. Ranges the /infrastructure folder for the shared dev topology (host + ports); the gateway:up/reload scripts source this file so Caddy's {$VAR:default} placeholders resolve. Edit in Infisical, not .env. */ -}}
{{- $pid := "266ba9d2-b8b3-41cd-a323-78da4a14da70" -}}

{{- range secret $pid "dev" "/infrastructure" -}}
{{ if .Comment }}# {{ .Comment }}
{{ end }}{{ .Key }}={{ .Value }}
{{ end -}}
